2d i)los atributos "tweets" y "timeline" para mi caso en particular, no deberán ser modificados ya que simplemente recibirán un tweet con
dos datos más el usuario que hizo retweet y a la hora en la que se hizo retweet, por lo que estos atributos no tienen ningún problema recibiendo 
esto. Si que habría que tener en cuenta que en en el ejercicio 2 la fecha se crea mediante la clase Date y no se pasa como una variable, por
lo que habría que utilizar el método adecuado para pasarlo como una variable a los atributos "tweets" y "timeline". (En el caso de que fuera
la clase DirectMessage y no Retweet si que habría que hacer modificaciones a timeline, pero ese no es el caso).

2d ii) igual que en el apartado anterior no era necesario cambiar el codigo del ejercicio 1 pero si el como se pasa del ejercicio 2; en este
ocurre lo mismo. Simplemente se encontrará que los "tweets"(del usuario) y "timelines"(de sus followers) se hallarán con un tweet con dos 
nuevas especificaciones, pero esto no invoca ningún problema ya que al usar el método ya se sabe los followers que tiene el usuario, 
por lo tanto ya sabe a quien mandarlo. (Igual que en el anterior si habría que modificar el método si se tratase de la clase DirectMessage
ya que no sabría como interpretar el "receiver" pero tampoco es el caso)
